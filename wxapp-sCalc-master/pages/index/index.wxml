<!--index.wxml-->
<view class="container">
  <view  bindtap="bindViewTap" class="userinfo">
    <image class="userinfo-avatar" src="{{userInfo.avatarUrl}}" background-size="cover"></image>
  </view>
  <view>
    <text class="userinfo-nickname">{{userInfo.nickName}}</text>
  </view>
  <view class="welcome-content">
    <text class="userinfo-nickname" >Welcome baby</text>
  </view>
   <!-- <view class="usermotto">
    <button type="default" size="{{primarySize}}" plain="{{plain}}" hover-class="button-hover"
        disabled="{{disabled}}" bindtap="toCalc"> {{motto}} </button>
  </view>
    <view>
    <button type="default" size="{{primarySize}}" plain="{{plain}}" hover-class="button-hover"
        disabled="{{disabled}}" bindtap="toFood"> 吃什么？</button>
  </view>
   <view>
    <navigator url="../content/content" class="github" hover-class="navigator-hover"> -->
    <!-- </navigator>
  </view> 
  <view>
    <button type="default" size="{{primarySize}}" plain="{{plain}}" hover-class="button-hover"
        disabled="{{disabled}}" bindtap="toMovie"> 电影资讯</button>
  </view>  -->
</view>
